<form method="post" action='/sessions/replied/<%= "#{params[:peep_id]}" %>'>
	<% @peep = Peep.first(:id => params[:peep_id]) %>
	<%= @peep.posted_by %>
	<%= @peep.content %>
	<%= @peep.when(@peep.created_at) %>
	Write Reply: <input type="text" name="content">
	<input type="submit" value="Reply to Peep">
</form>